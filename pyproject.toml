[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ow"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["argcomplete", "jinja2", "tomli-w"]

[tool.setuptools.packages.find]
include = ["ow*"]

[project.scripts]
ow = "ow.__main__:main"
